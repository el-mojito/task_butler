{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Start Home Assistant",
            "type": "shell",
            "command": "source .venv/bin/activate && hass -c config",
            "problemMatcher": []
        }
    ]
}